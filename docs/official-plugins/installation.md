# Installation

Open Pentest → Plugins → Official Catalog, review manifest/capabilities/digest, select one package, and choose explicit installation. Installation copies the verified package into disabled local storage without importing it. Trust approval, capability approval, enablement, and load are separate actions. Missing optional tools do not install automatically.
