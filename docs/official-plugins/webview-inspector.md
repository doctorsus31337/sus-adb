# WebView Security Inspector

Static analysis reports candidates from selected manifest/source inputs. Runtime support supplies an editable, untrusted, unloaded observation agent using the selected Frida runtime only after explicit review/trust/load. It truncates metadata and does not capture full pages, credentials, alter arguments/returns or SSL decisions, bypass TLS/pinning, or inject page JavaScript. Findings require operator confirmation.
