# Script Studio

Script Studio is the editable Frida source workspace. Imported scripts are untrusted; validation never rewrites or executes source. Advisory warnings do not block saving, while loading requires trust/runtime safeguards. Review scripts for sensitive collection and state changes.
