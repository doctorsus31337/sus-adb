# Findings and Reporting

Findings use explicit lifecycle, severity, confidence, evidence/timeline relationships, and retests. Offline HTML, Markdown, and JSON reports are deterministic, escaped, locally hashed, and never uploaded. Redaction affects snapshots, not case source data.
