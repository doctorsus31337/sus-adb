# APK Laboratory

The APK Lab preserves originals and creates derived decoded, instrumented, rebuilt, aligned, and signed artifacts. Gadget binaries and keystores are user-supplied and never downloaded or stored in metadata. Build, sign, install, and launch are separate actions.
