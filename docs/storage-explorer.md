# Storage Explorer

Storage access modes are explicit normal shell, run-as, or root. Preferences are read-only, SQLite permits parameterized SELECT only, provider queries are bounded/read-only, and snapshots include only selected paths within configured limits.
