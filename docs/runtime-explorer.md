# Runtime Explorer

Runtime Explorer discovers Java/native metadata through the existing selected-target Frida runtime. Generated hooks are previewed and remain unloaded until explicit confirmation. State-changing hooks require active authorized scope and remain visible/unloadable.
