# Troubleshooting

Run `python main.py --diagnostics` and review local logs. Missing optional tools are guidance, not startup failures. For an unclean shutdown, start normally or with third-party plugins disabled. Never delete case/evidence data as a troubleshooting shortcut.
