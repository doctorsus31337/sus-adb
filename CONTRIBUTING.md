# Contributing

Work from a feature branch, preserve authorization safeguards, keep GUI and device/tool logic separate, and never commit generated cases or secrets. Install `requirements-dev.txt`, run `python scripts/run_release_checks.py`, and describe platform-specific validation. Do not execute real device tooling in automated tests.
