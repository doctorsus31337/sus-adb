# Hello Local Example

This package demonstrates read-only dashboard, menu, report-section, and Script Studio asset declarations. It is untrusted and disabled by default. It performs no device, network, subprocess, or external-file action and never loads automatically.
