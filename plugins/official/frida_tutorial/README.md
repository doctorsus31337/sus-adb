# Frida Foundations

An official, disabled-by-default local course. It requests no capabilities,
uses synthetic examples and harmless optional practice, and never contacts a
device, selects a target, launches Frida, loads a script, or downloads content
while lessons are browsed.
