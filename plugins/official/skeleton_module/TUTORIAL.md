# Step-by-step tutorial

1. Export the template. 2. Choose a unique stable ID and never reuse `susadb.skeleton-module`. 3. Change the name/version. 4. Keep API `1.1` for interactive contracts. 5. Synchronize unique contribution IDs in manifest and registration. 6. Request minimal real capabilities. 7. Return immutable specs/results. 8. Start no work during construction. 9. Use bounded progress/cancellation. 10. Test with fakes and the Workbench. 11. Install and explicitly review the new digest. Export never installs, trusts, enables, loads, opens, or executes the copy.
